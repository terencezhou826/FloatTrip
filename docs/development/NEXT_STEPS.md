# Next Steps

Updated: 2026-08-22 12:22:28 +08:00

1. After approval, create an M2A Git checkpoint. Do not commit automatically.
2. Enter M2B only under a separate explicit instruction and scope retrieval to production-eligible Claims. Keep Source/Claim/Evidence traceability and promotion qualification in every returned result.
3. Define source archival/versioning policy before relying on public URLs for long-term production citations.
4. Keep `changzhi.claim.fajiushan-yandi-residence` in `review_required` and `internal_only` unless independent historical evidence is supplied.
5. Track `RuntimeEndToEndTests.test_two_plans_execute_concurrently_without_merging` as `KNOWN_FLAKY`; it did not reproduce and Runtime was not modified.

Boundary to preserve: M2A contains no RAG, embedding, vector database, LLM extraction/review, Planning prompt, Runtime, SSE, Story, Experience, Commerce, Video, frontend, or mobile integration. Verified mythology remains distinct from verified historical fact.
